# import email
from api.models import Candidate


xyz = {
    "email":"stephanieporter@example.net"
}
candidates = Candidate.objects.all()[200]


survey.save(update_fields=["active"]) 

candidates.update(**xyz)